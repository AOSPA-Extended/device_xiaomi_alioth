add_hwservice(hal_nfc_default, nxpese_hwservice)
add_hwservice(hal_nfc_default, nxpnfc_hwservice)

# Allow NFC hal to access vendor_nfc_vendor_data_file
r_dir_file(hal_nfc_default, vendor_nfc_vendor_data_file)
